# 记账本项目技术演进与规划

## 一、项目背景与技术演进

### 1. 起点：静态前端应用

最初使用 Claude 生成一个静态的记账本前端应用，用于个人记账管理。

### 2. 全栈化：Next.js 框架

为了实现更完整的功能，将项目迁移到 Next.js 框架，从纯静态应用转变为全栈项目。

### 3. 界面美化：frontend-design 插件

觉得页面太单调，通过指令在 Discover 中找到 frontend-design 插件来修改界面，提升用户体验。

### 4. 多人分账需求

随着功能需求增加，需要实现多人形成小组分账的功能：

- 需要数据库存储用户、小组、账单数据
- 需要部署上线供他人使用
- 静态应用已无法满足需求

### 5. 数据库选型历程

#### 阶段一：Supabase

- 考虑使用 Supabase（第三方数据库服务）
- **问题**：Supabase 发送邮件注册的配置较为复杂
- **问题**：数据安全性顾虑（依赖第三方服务）
- **问题**：需要自己编写 SQL 创建表结构，大大增加了后端小白的上手难度

**关于 Supabase 发送邮件的能力：**

Supabase 确实支持发送邮件功能，但方式如下：

- **Supabase Auth 自带邮件服务**：配置相对简单，支持邮件验证、密码重置等
- **需要配置 SMTP 或使用第三方邮件服务**：如果需要自定义邮件模板或使用自己的域名，需要配置 SMTP 或集成 SendGrid、Resend 等服务
- **免费额度限制**：Supabase 的免费计划有 API 调用次数限制

#### 阶段二：SQLite（当前方案）

**最终选择 SQLite 的原因与优点：**

| 优点       | 说明                                                 |
| ---------- | ---------------------------------------------------- |
| 零配置     | 无需搭建数据库服务器，开箱即用                       |
| 部署简单   | 文件即数据库，适合 Vercel/Netlify 等 Serverless 部署 |
| 数据可控   | 数据完全存储在本地文件，安全性有保障                 |
| 性能良好   | 读写性能优秀，单用户或小规模多人使用足够             |
| 迁移简单   | Drizzle ORM 提供跨数据库迁移支持                     |
| 无额外成本 | 无需支付数据库服务费用                               |
| 学习友好   | 用 TypeScript 定义 Schema，比手写 SQL 更直观         |

---

## 二、学习收获

通过这个项目，对后端知识有了初步了解：

- 学会了使用 Drizzle ORM 定义数据模型（Schema）
- 理解了数据库表结构设计（users、profiles 等表关系）
- 掌握了 API 路由开发（Next.js API Routes）
- 了解了用户认证流程（NextAuth）
- 认识了密码加密与安全存储（bcrypt）
- 不再只是专注于前端内容，开始理解前后端协作的全貌

---

## 三、当前技术栈

| 类别     | 技术                                    |
| -------- | --------------------------------------- |
| 前端框架 | Next.js 14 (App Router)                 |
| UI 框架  | Tailwind CSS + frontend-design 设计系统 |
| 认证方案 | NextAuth.js v5 (Credentials Provider)   |
| ORM      | Drizzle ORM                             |
| 数据库   | SQLite (better-sqlite3)                 |
| 密码加密 | bcryptjs                                |

---

## 四、后续功能规划

### 已规划功能

1. **注册登录增强**（待实现）
   - 用户名 + 密码 + 邮箱验证
   - 完整密码强度检查
   - 邮箱验证流程

2. **多人分账功能**（核心需求）
   - 用户注册与认证
   - 创建/加入小组
   - 小组内账单记录与分账计算
   - 账单分享与结算

### 技术准备

当前 SQLite 方案已具备支持多人分账的基础：

- Drizzle ORM 支持多表关联查询
- NextAuth 可扩展支持小组/团队概念
- 可通过字段关联实现小组-用户-账单关系

---

## 五、总结

本项目从简单的静态记账工具，逐步演进为：

1. 全栈 Next.js 应用
2. 美观的 UI 界面
3. 本地优先的 SQLite 数据存储
4. 为多人协作分账功能做好准备

下一步重点是完善用户认证系统，为多人分账功能奠定基础。
